import React from 'react'

function SocialMediaIcon ({ divClass, classa, classI }) {
    return (
        <li className={divClass}>

            <a className={classa}>
                <i className={classI}> </i>
            </a>

        </li>

    )
}

export default SocialMediaIcon
